linters:
    linters_with_defaults(line_length_linter(80),
    (object_usage_linter(interpret_glue = TRUE)),
    (object_name_linter = NULL), (indentation_linter(indent = 4)),
    (commented_code_linter=NULL)) # see vignette("lintr")
    # linters_with_tags(tags = c("best practices", "readability", "efficiency",
    # "consistency")) exclude: regex:box::use\(.*\)*nolint
encoding: "UTF-8"
